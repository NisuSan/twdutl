version: 2.1

orbs:
  node: circleci/node@5.0.0

jobs:
  build:
    docker:
      - image: cimg/node:17.4.0
    steps:
      - checkout
      - node/install-packages

workflows:
  default:
    jobs:
      - build
